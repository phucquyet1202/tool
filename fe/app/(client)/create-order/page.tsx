import React from "react";

import CreateOrderForm from "@/components/client/order/CreateOrderForm";

type Props = {};

const page = (props: Props) => {
  return (
    <>
      <CreateOrderForm />
    </>
  );
};

export default page;
